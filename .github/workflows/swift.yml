name: Build and Release

on:
  workflow_dispatch:
    inputs:
      create_nightly:
        description: 'Create nightly release'
        type: boolean
        default: false

jobs:
  build:
    runs-on: macos-latest
    outputs:
      artifact: ${{ steps.upload-artifact.outputs.artifact }}
    steps:
      - name: Prepare workspace
        shell: bash
        run: |
          current="${GITHUB_WORKSPACE}"
          while [ -n "${current}" ] && [ "${current}" != "/" ]; do
            echo "::add-mask::${current}"
            current=$(dirname "${current}")
          done

      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          clean: true
          fetch-depth: 1

      - name: Checkout Firefox submodule
        run: |
          echo "Starting Firefox submodule checkout..."
          git submodule update --init --depth 1 --recursive engine/firefox
          echo "Firefox submodule checkout completed with status: $?"

      - name: Checkout idevice submodule
        run: |
          echo "Starting idevice submodule checkout..."
          git submodule update --init --depth 1 --recursive support/idevice
          echo "idevice submodule checkout completed with status: $?"

      - name: Update Gecko
        run: |
          echo "Starting Gecko update..."
          ./tools/development/update-gecko.sh
          echo "Gecko update completed with status: $?"

      - name: Apply Gecko patches
        run: |
          echo "Starting patch application..."
          ./tools/development/apply-patches.sh
          echo "Patches applied with status: $?"

      - name: Build idevice
        run: |
          echo "Starting idevice build..."
          ./tools/development/build-idevice.sh
          echo "idevice build completed with status: $?"

      - name: Build the app
        run: |
          echo "Starting app build..."
          ./tools/release/build-app.sh
          echo "App build completed with status: $?"

      - name: Create .ipa file
        run: |
          echo "Starting .ipa creation..."
          ./tools/release/create-ipa.sh
          echo ".ipa creation completed with status: $?"

      - name: Tar build artifact
        run: |
          echo "Starting tar compression..."
          tar -czf ./dist/Reynard.xcarchive.tar.gz -C ./dist Reynard.xcarchive
          echo "Tar compression completed with status: $?"

      - name: Upload build artifact
        id: upload-artifact
        uses: actions/upload-artifact@v4
        with:
          name: Reynard.xcarchive
          path: ./dist/Reynard.xcarchive.tar.gz

      - name: Upload .ipa artifact
        uses: actions/upload-artifact@v4
        with:
          name: Reynard.ipa
          path: ./dist/Reynard.ipa

      - name: Clean up workspace
        if: always()
        shell: bash
        run: |
          if [ -n "${GITHUB_WORKSPACE}" ]; then
            find "${GITHUB_WORKSPACE}" -mindepth 1 -maxdepth 1 -exec rm -rf -- {} +
          fi

  release:
    runs-on: ubuntu-latest
    needs: build
    if: github.event.inputs.create_nightly && github.event.inputs.create_nightly == 'true'
    permissions:
      contents: write
    steps:
      - name: Prepare workspace
        shell: bash
        run: |
          current="${GITHUB_WORKSPACE}"
          while [ -n "${current}" ] && [ "${current}" != "/" ]; do
            echo "::add-mask::${current}"
            current=$(dirname "${current}")
          done

      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          clean: true

      - name: Download .ipa artifact
        uses: actions/download-artifact@v4
        with:
          name: Reynard.ipa
          path: ./dist

      - name: Get information for release
        id: version
        run: |
          CURRENT_VERSION=$(grep 'CURRENT_VERSION' ./browser/Configuration/Reynard.xcconfig | cut -d '=' -f2 | tr -d ' ')
          CURRENT_BUILD=$(date +%Y%b%d | tr '[:lower:]' '[:upper:]')
          ENGINE_REVISION=$(cat ./engine/release.txt)
          echo "current_version=$CURRENT_VERSION" >> $GITHUB_OUTPUT
          echo "current_build=$CURRENT_BUILD" >> $GITHUB_OUTPUT
          echo "engine_revision=$ENGINE_REVISION" >> $GITHUB_OUTPUT

      - name: Create nightly release
        uses: andelf/nightly-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          body: |
            **Build:** ${{ steps.version.outputs.current_build }}
            **Version:** ${{ steps.version.outputs.current_version }}
            **Engine:** [${{ steps.version.outputs.engine_revision }}](https://github.com/mozilla-firefox/firefox/commit/${{ steps.version.outputs.engine_revision }})
            **Commit:** [${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})

            This is a nightly release [created automatically with GitHub Actions workflow](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/attempts/${{ github.run_attempt }}).

            Before using this release, please read the installation instruction in the README at [#Installation](https://github.com/minh-ton/reynard-browser#installation).
          files: |
            ./dist/Reynard.ipa

      - name: Clean up workspace
        if: always()
        shell: bash
        run: |
          if [ -n "${GITHUB_WORKSPACE}" ]; then
            find "${GITHUB_WORKSPACE}" -mindepth 1 -maxdepth 1 -exec rm -rf -- {} +
          fi
